# eSanté Simulator Configuration
# Copy this file to .env and adjust values as needed

# Number of patients to simulate
NUM_PATIENTS=10

# Emit vitals every N seconds (faster for testing)
EMIT_INTERVAL_SECONDS=10

# Generate alert/abnormal values every N seconds
ALERT_INTERVAL_SECONDS=60
