# Simulator Configuration
NUM_PATIENTS=10
EMIT_INTERVAL_SECONDS=10
ALERT_INTERVAL_SECONDS=60

# Docker Build Configuration
# Use Dockerfile.corporate if you're behind a corporate proxy with custom CA certificates
# SIMULATOR_DOCKERFILE=Dockerfile.corporate
SIMULATOR_DOCKERFILE=Dockerfile
